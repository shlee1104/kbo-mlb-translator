# Core pipeline (Phase 1: audit, validation, crosswalk)
pandas>=2.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
tabulate>=0.9          # pandas .to_markdown() in the audit report

# MLB season stats (optional - the pipeline degrades gracefully without it)
pybaseball>=2.2

# Model and app (Days 4-6)
numpy>=1.24
scikit-learn>=1.3
statsmodels>=0.14
streamlit>=1.30
altair>=5.0

# Development
pytest>=7.4
